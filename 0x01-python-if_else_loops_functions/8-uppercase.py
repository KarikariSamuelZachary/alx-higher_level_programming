#!/usr/bin/python3
def uppercase(str)
    for i in range(len(str)):
        n = ord(str[i])
        if n >= 97 and n <= 112:
            n = n - 32
        print("{:c}".format(temp), end='')
    print()
